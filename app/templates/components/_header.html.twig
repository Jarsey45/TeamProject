<div class="header">
	<a class="unstyled_link" href="{{path('app_home')}}">
		<div class="logo">FishingSpot</div>
	</a>
	<form class="post-container" action="{{ path('api_post_add') }}" method="post">
		<input type="text" class="post-input" placeholder="How's the fishing going?" name="content">
		<button class="post-button">Plum...</button>
	</form>
	<button class="profile-button">Account</button>
</div>
