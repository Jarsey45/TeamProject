<form class="comment-container" action="{{ path('api_comment_add', {'id': postID}) }}" method="post">
		<input type="text" class="comment-input" placeholder="Comment" name="content">
		<button class="comment-button">Plum...</button>
</form>
